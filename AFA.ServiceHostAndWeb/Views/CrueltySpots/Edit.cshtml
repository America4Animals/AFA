@model AFA.ServiceHostAndWeb.Models.CrueltySpotDetailModel

@{
    ViewBag.Title = "Edit Cruelty Spot";
}

@using (Html.BeginForm("Edit", "CrueltySpots", FormMethod.Post, new { @class = "form-horizontal" }))
{
    @Html.Partial("AddOrEdit", Model)

    @Html.HiddenFor(m => m.Id)
}
